функция а(a,знач n) 
  f=""+1;
	Выполнить("Сообщить("+f+null+")");
  пока 1 цикл
    если а > b тогда
      прервать;
    конецесли;
  конеццикла;
	ДобавитьОбработчик Эл.ПриЗаписи, ПриЗаписиДокумента;
	УдалитьОбработчик Эл.ПриЗаписи, ПриЗаписиДокумента;

	Если истина = Неопределено Тогда
	
		
	
	КонецЕсли; 

конецфункции
